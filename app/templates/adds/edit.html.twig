{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.adds_edit'|trans({'%id%': adds.id|default('')}) }}
{% endblock %}

{% block body %}
    {{ include('menu.html.twig') }}
    <h2>{{ 'title.adds_edit'|trans({'%id%': adds.id|default('')}) }}</h2>
    {% include 'default/_form.html.twig' with {
        page_title: 'title.adds_edit'|trans({'%id%': add.id|default('')}),
        form: form,
        submit_label: 'action.edit'|trans,
        back_to_list_path: 'adds_index',
    } %}
{% endblock %}